{
  "name": "wlas",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev:server\" \"npm run dev:frontend\"",
    "dev:server": "bun run dev -w @wlas/server",
    "dev:frontend": "bun run dev -w @wlas/frontend"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "bun-types": "^1.3.1",
    "dotenv-cli": "^10.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
